# Intital setup

```shell
yarn init
yarn add --dev typescript
tsc --init
yarn add --dev ts-node
yarn add --dev nodemon
yarn add octokit
npx tsc --init
mkdir src
cd src
touch index.ts
yarn add progress
yarn add @types/progress
```

# Run the script

```shell
yarn start
```
